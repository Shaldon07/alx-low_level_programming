#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer that is to be checked
 * Return: absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
